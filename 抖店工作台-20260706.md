# 抖店工作台 经验记录 (2026-07-06)

## 本次修改总结

### 1. 饼图百分比标注方式
- ❌ `generateLabels` → 导致页面空白 (Chart.js v4 bug)
- ✅ `afterDraw` 插件 + 预计算 labels 数组 → 稳定

### 2. 多游戏架构
- `data.json.games[]` 定义游戏列表
- 每场 session 有 `game` 字段
- 全店汇总：所有游戏叠加（不受筛选影响）
- 基础数据报表：游戏下拉框 + 日期双筛选

### 3. 游戏对比柱状图
- 多数据集分组柱状图（每个游戏一个 dataset）
- 图例在底部，`usePointStyle:true`
- 柱顶标注遍历所有 dataset

### 4. 空数据处理
- 无数据的游戏不提前 `return`
- `renderSingleDay(null)` + `renderMultiDay([])` 安全处理
- `assessRisk` 首行空数据守卫
- 除零守卫：`cnt>0 ? div/cnt : 0`

### 5. setTimeout 括号平衡
- `setTimeout(function(){ ... }, N)` 嵌套 `if(){...}` 时容易少 `}`
- 检查：统计函数体内 `{` 和 `}` 数量

### 6. 单日支付渠道数据源
- ❌ 全局 `payData` 变量（硬编码 7/2 数据）
- ✅ 从当前场次 `s.payment` 读取

### 7. AI 图片去水印
- 用 Agnes Image 2.1 Flash image-to-image
- 大图裁剪区域再送 API，避免 SSL EOF

## 推送记录
- `5b51e75` 全店汇总支付渠道删除汇总卡片
- `989de70` fix: setTimeout缺闭合括号
- `70ec7d9` fix: 无数据时assessRisk崩溃+清空旧图表
- `ef3aff5` 无数据时显示0卡片
